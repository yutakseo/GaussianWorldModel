# Configuration for droid dataset
# _target_: gaussianwm.processor.datasets.DroidDataset
dataset_name: droid

# Data parameters
data_path: ${oc.env:GWM_PATH}/data/

# Dataset parameters
# Paper Table A1: 12-frame sequences, 2 context frames, 10-step rollout.
segment_length: 12
context_length: 2
# segment_length: 2
# context_length: 1
action_dim: 10  # DROID uses 10-dimensional actions (absolute coordinates + gripper)
image_size: 128  # Matches crop size in config
# shuffle_buffer_size: 100_000
shuffle_buffer_size: 10  # fast debug

# Data processing
augment: false  # Temporarily disabled for compatibility
split_fractions:
  train: 0.90
  val: 0.05
  test: 0.05
load_all_data_for_training: false
max_val_samples: null
seed: ${seed}

# Camera and action handling
# The current DROID adaptation uses the paper-supported single-view path.
camera_keys: ["primary"]
action_keys: ["actions"]

# DROID-specific RLDS parameters
future_action_window_size: 15
subsample_length: 100
batch_size: null
traj_transform_threads: 48
traj_read_threads: 48
